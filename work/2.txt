class FieldMeta
def init(self, metastr: str):
meta json loads(metastr)
if isinstance(meta, str):
self instance get_instance (meta[])
else
option= meta['type']get('option')
if option:
selfinstance get instance (meta['type'I['name'l,**option
else
selfinstance get_instance(meta[]0
self.unique= meta. get('unique', False)